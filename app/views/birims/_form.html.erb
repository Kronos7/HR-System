<%= form_for(@birim) do |f| %>
  <% if @birim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@birim.errors.count, "error") %> prohibited this birim from being saved:</h2>

      <ul>
      <% @birim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :icerik %><br>
    <%= f.text_field :icerik %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
