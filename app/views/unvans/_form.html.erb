<%= form_for(@unvan) do |f| %>
  <% if @unvan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unvan.errors.count, "error") %> prohibited this unvan from being saved:</h2>

      <ul>
      <% @unvan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :icerik %><br>
    <%= f.text_field :icerik %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
